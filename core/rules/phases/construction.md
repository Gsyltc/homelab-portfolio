# Règles — couche `phase` : Construction

Règles propres à la phase **Construction** (COMMENT : production des livrables détaillés, vérification). Chargées **à la demande** quand la phase Construction est déclenchée.

Précédence : sous `workspace` et `project`, au-dessus de `scope`.

> Aucune règle apprise pour l'instant. Les règles confirmées et non conflictuelles seront ajoutées ci-dessous, au format `RULE-PH-NNN`.
