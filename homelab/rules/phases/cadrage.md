# Règles — couche `phase` : Cadrage et Paramètres

Règles propres à la phase **Cadrage et Paramètres** (Phase 1 : réception, arbitrage, collecte des paramètres requis). Chargées **à la demande** quand la phase Cadrage est déclenchée.

Pendant Homelab de [`../../core/rules/phases/inception.md`](../../core/rules/phases/inception.md) (correspondance AI-DLC : Inception).

Précédence : sous `global` et `stack`, au-dessus de `scope`.

> Aucune règle apprise pour l'instant. Les règles confirmées et non conflictuelles seront ajoutées ci-dessous, au format `RULE-PH-NNN`.
