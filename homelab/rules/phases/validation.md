# Règles — couche `phase` : Validation et Déploiement

Règles propres à la phase **Validation et Déploiement** (Phase 3 : passage en revue, validation humaine, dépôt fichiers, déploiement Kestra, clôture). Chargées **à la demande** quand la phase Validation est déclenchée.

Précédence : sous `global` et `stack`, au-dessus de `scope`.

> Aucune règle apprise pour l'instant. Les règles confirmées et non conflictuelles seront ajoutées ci-dessous, au format `RULE-PH-NNN`.
